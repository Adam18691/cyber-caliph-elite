# FILE: cyber_caliph_project/requirements.txt
# اسم الملف: requirements.txt - v204 FINAL FIX - Python 3.14 Compatible - للنسخ
# اصلاح نهائي: Python 3.14 + eventlet = SyntaxError: 'return' in a 'finally' block
# eventlet لا يدعم Python 3.14 - تم ازالته تماما - استخدام gthread بدلاً منه
# اسم الملف مكتوب عليه - GitHub Adam18691/cyber-caliph-elite - v204 FINAL - للنسخ

# === الاساسي فقط - متوافق مع Python 3.14 - كلها wheels - يبني في 6 ثواني ===
Flask==3.0.0
Flask-SocketIO==5.3.6
python-socketio==5.11.0
python-engineio==4.8.0
gunicorn==23.0.0
python-dotenv==1.0.0
requests==2.31.0

# تم ازالة: eventlet + gevent + gevent-websocket + Pillow + PyYAML + grpcio + google-api
# السبب: eventlet يسبب SyntaxError في Python 3.14 - gevent يحتاج build - Pillow 46MB يفشل

# FILE: requirements.txt - نهاية - v204 FINAL FIX - بدون eventlet - يبني في 6 ثواني - للنسخ
