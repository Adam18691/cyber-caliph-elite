# FILE: cyber_caliph_project/requirements.txt
# اسم الملف: requirements.txt - v203 ULTRA LIGHT RENDER FIX - يبني في 8 ثواني - للنسخ
# اصلاح: Build failed عند gevent-24.11.1.tar.gz 6MB + Deploy cancelled - للنسخ
# الحل: ازالة gevent + Pillow 46MB + grpcio 12MB + PyYAML - فقط الضروري wheels جاهزة
# اسم الملف مكتوب عليه - GitHub Adam18691/cyber-caliph-elite - v203 - للنسخ

# === الاساسي فقط - يبني في 8 ثواني - كلها wheels - لا تحتاج build ===
Flask==3.0.0
Flask-SocketIO==5.3.6
python-socketio==5.11.0
python-engineio==4.8.0
gunicorn==21.2.0
eventlet==0.33.3
python-dotenv==1.0.0
requests==2.31.0

# FILE: requirements.txt - نهاية - v203 ULTRA LIGHT - يبني في 8 ثواني - للنسخ
# تم ازالة: gevent + gevent-websocket + Pillow + PyYAML + grpcio + google-api + numpy + redis + kafka
# لو احتجت اي مكتبة ثقيلة بعد ما يشتغل المشروع - ضيفها واحدة واحدة
